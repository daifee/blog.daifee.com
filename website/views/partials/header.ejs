<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title><%= locals.title %></title>
<!-- 新 Bootstrap 核心 CSS 文件 -->
<link rel="stylesheet" href="https://cdn.bootcss.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
<link rel="stylesheet" href="/static/css/common.css">
<% if (locals.styles) { %>
  <% locals.styles.forEach(function (value) { %>
    <link rel="stylesheet" href="<%= value %>">
  <% }) %>
<% } %>
</head>

  <body>
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">blog.daifee.com</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% if (_user.id) { %>
              <li><a href="/users/<%= _user.name %>">主页</a></li>
            <% } %>
            <li><a href="/explore">发现</a></li>
            <% if (_user.id) { %>
              <li><a href="/articles/new">写文章</a></li>
            <% } %>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <% if (_user.id) { %>
              <li><a href="/logout">退出</a></li>
            <% } else { %>
              <% if (locals.referer) { %>
                <li><a href="/login?referer=<%= locals.referer %>">登录</a></li>
                <li><a href="/register?referer=<%= locals.referer %>">注册</a></li>
              <% } else { %>
                <li><a href="/login">登录</a></li>
                <li><a href="/register">注册</a></li>
              <% } %>
            <% } %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>

    <div class="container">

